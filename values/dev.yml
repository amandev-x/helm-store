# Dev Environment
replicaCount: 1

image:
  tag: latest

reosources:
  requests:
    memory: 128Mi
    cpu: 100m

  limits:
    memory: 160Mi
    cpu: 200m

config:
  logLevel: debug

redis:
  auth:
    enabled: false
  master:
    persistence:
      enabled: false 

postgresql:
  auth:
    postgresPassword: helmstore123
    database: products 
  
  primary:
    persistence:
      enabled: false

